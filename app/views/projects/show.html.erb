<header>
  <p class="date"><%= @project.created_at.strftime("%A, %B, %d") %></p>
  <h1><%= @project.title %></h1>  
</header>

<div id="content">
  <%= markdown @project.description %>
  <a href='<%=  @project.link %>' target='_blank'>Visit <%= @project.title %></a>
</div>
