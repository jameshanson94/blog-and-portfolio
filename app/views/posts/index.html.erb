<h1>Posts</h1>

<% @posts.each do |post| %>
  <div class="post">
    <p class="date"><%= post.created_at.strftime("%A, %B %d") %></p>
    <p class="title"><%= link_to post.title, post_path(post) %></p>
  </div>  
<% end %>


<%= will_paginate @posts %>
